---
const { tableId } = Astro.props;
---
<script is:inline define:vars={{tableId}}>
$(document).ready( function () {
    $(tableId).DataTable({
        stateSave: true,
        paging: false,
        order: [[0, 'asc'], [1, 'asc']]
    });
} );
</script>
